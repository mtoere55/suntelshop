{
  "name": "suntelshop-full-v1",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "start": "node server/index.mjs",
    "seed": "node scripts/seed.mjs --force",
    "check": "npm run build && node --check server/index.mjs"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "express": "latest",
    "nodemailer": "^8.0.7",
    "react": "latest",
    "react-dom": "latest",
    "vite": "latest"
  }
}
